<Window x:Class="iikoServiceHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iikoServiceHelper_v2" Height="600" Width="950" MinWidth="740" MinHeight="530" WindowStartupLocation="Manual"
        Background="#121212" Foreground="White" Icon="pack://application:,,,/Logo.ico"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:conv="clr-namespace:iikoServiceHelper.Converters">
    <Grid Background="{DynamicResource BrushWindowBackground}">
        <Grid.Resources>
            <conv:BoolToStringConverter x:Key="BoolToStringConverter"/>
            <Style x:Key="Win11ToggleStyle" TargetType="{x:Type CheckBox}">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Height" Value="10" />
                <Setter Property="Width" Value="20" />
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Grid>
                                <Border x:Name="Track" CornerRadius="5" Height="10" Background="{DynamicResource BrushCounterBackground}" BorderBrush="{DynamicResource BrushCounterBorder}" BorderThickness="1" />
                                <Border x:Name="Thumb" CornerRadius="4" Height="8" Width="8" Background="#FAFAFA" HorizontalAlignment="Left" Margin="1,0,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="0.5" Direction="270" Color="Black" Opacity="0.2" BlurRadius="1"/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Track" Property="Background" Value="{DynamicResource BrushAccent}" />
                                    <Setter TargetName="Track" Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Storyboard.TargetName="Thumb" Storyboard.TargetProperty="Margin" To="11,0,0,0" Duration="0:0:0.15">
                                                    <ThicknessAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></ThicknessAnimation.EasingFunction>
                                                </ThicknessAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Storyboard.TargetName="Thumb" Storyboard.TargetProperty="Margin" To="1,0,0,0" Duration="0:0:0.15">
                                                    <ThicknessAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></ThicknessAnimation.EasingFunction>
                                                </ThicknessAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Thumb" Property="Background" Value="#E0E0E0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <!-- Header / Title Area Decoration -->
        <Border Height="1" VerticalAlignment="Top" Margin="20,50,20,0" Background="#333333"/>

        <TabControl x:Name="MainTabControl" Background="Transparent" BorderThickness="0" Margin="10">
            <!-- Tab 1: Reference (ReadOnly) -->
            <TabItem Header="КОМАНДЫ" PreviewMouseLeftButtonDown="CommandsTab_PreviewMouseLeftButtonDown">
                <Grid Margin="10,0,10,10">
                    <DataGrid x:Name="ReferenceGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" FontSize="14" SelectionUnit="Cell" Focusable="False"
                              AlternationCount="2" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource BrushDataGridGridLines}" BorderThickness="0" RowHeight="33">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="{DynamicResource BrushDataGridBackground}" />
                                <Style.Triggers>
                                    <Trigger Property="AlternationIndex" Value="1">
                                        <Setter Property="Background" Value="{DynamicResource BrushDataGridAlternateBackground}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="СОЧЕТАНИЕ" Binding="{Binding Keys}" Width="250">
                                <DataGridTextColumn.CellStyle>
                                    <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.CellStyle>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextAlignment" Value="Left"/>
                                        <Setter Property="Margin" Value="12,0,0,0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="ОПИСАНИЕ" Binding="{Binding Desc}" Width="*">
                                <DataGridTextColumn.CellStyle>
                                    <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                                        <Setter Property="ToolTip" Value="{Binding FullCommand}" />
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.CellStyle>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="TextAlignment" Value="Left"/>
                                        <Setter Property="Margin" Value="12,0,0,0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Tab 2: Notes -->
            <TabItem Header="ЗАМЕТКИ">
                <Grid Margin="3">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,-5,0,0">
                        <Button Content="🔍+" Command="{Binding SettingsViewModel.ZoomInCommand}" Width="24" Height="24" Margin="1" Padding="0" FontSize="10" ToolTip="Увеличить"/>
                        <Button Content="🔍-" Command="{Binding SettingsViewModel.ZoomOutCommand}" Width="24" Height="24" Margin="1" Padding="0" FontSize="10" ToolTip="Уменьшить"/>
                    </StackPanel>
                    <TextBox Grid.Row="1" x:Name="txtNotes" Style="{StaticResource ThemedTextBox}" AcceptsReturn="True" TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto" Text="{Binding NotesViewModel.Content, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="{Binding SettingsViewModel.NotesFontSize}" Margin="0,10,0,10"/>
                </Grid>
            </TabItem>

            <!-- Tab 4: Custom Command Editor -->
            <TabItem x:Name="CommandEditorTab" Header="РЕДАКТОР КОМАНД" Visibility="Collapsed">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <DataGrid x:Name="CustomCommandsGrid" Grid.Row="0" AutoGenerateColumns="False" CanUserAddRows="False" FontSize="12" BeginningEdit="CustomCommandsGrid_BeginningEdit">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                        <Setter Property="Foreground" Value="Gray"/>
                                        <Setter Property="ToolTip" Value="Это стандартная команда, ее редактирование заблокировано."/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="СОЧЕТАНИЕ" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Trigger}" VerticalAlignment="Center" Margin="5,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource ThemedTextBox}" Text="{Binding Trigger, UpdateSourceTrigger=PropertyChanged}" 
                                                 GotFocus="HotkeyTextBox_GotFocus" LostFocus="HotkeyTextBox_LostFocus"
                                                 IsReadOnly="True" IsReadOnlyCaretVisible="False"
                                                 ToolTip="Кликните и нажмите сочетание клавиш для записи."/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="ОПИСАНИЕ" Width="250">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Description}" VerticalAlignment="Center" Margin="5,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource ThemedTextBox}" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridComboBoxColumn Header="ТИП" SelectedItemBinding="{Binding Type}" Width="100">
                                <DataGridComboBoxColumn.ElementStyle>
                                    <Style TargetType="ComboBox" BasedOn="{StaticResource ThemedComboBox}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                                <Setter Property="IsEnabled" Value="False"/>
                                                <Setter Property="Opacity" Value="0.6"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridComboBoxColumn.ElementStyle>
                                <DataGridComboBoxColumn.EditingElementStyle>
                                    <Style TargetType="ComboBox" BasedOn="{StaticResource ThemedComboBox}"/>
                                </DataGridComboBoxColumn.EditingElementStyle>
                                <DataGridComboBoxColumn.ItemsSource>
                                    <x:Array Type="sys:String">
                                        <sys:String>Reply</sys:String>
                                        <sys:String>Bot</sys:String>
                                        <sys:String>System</sys:String>
                                    </x:Array>
                                </DataGridComboBoxColumn.ItemsSource>
                            </DataGridComboBoxColumn>
                            <DataGridTemplateColumn Header="СОДЕРЖИМОЕ" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Content}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource ThemedTextBox}" Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" 
                                                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MaxHeight="100"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <WrapPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button x:Name="btnUnlockCommands" Content="🔓" Click="ToggleSystemCommandsUnlock_Click" Width="40" Height="35" ToolTip="Разблокировать команды для редактирования (временно)" Margin="0,0,5,0"/>
                        <Button Content="Экспорт" Click="ExportCommands_Click" Width="90" Height="35" Margin="0,0,5,0"/>
                        <Button Content="Импорт" Click="ImportCommands_Click" Width="90" Height="35" Margin="0,0,5,0"/>
                        <Button Content="Добавить" Click="AddCustomCommand_Click" Width="120" Height="35" Margin="0,0,5,0"/>
                        <Button Content="Удалить" Click="DeleteCustomCommand_Click" Width="120" Height="35" Margin="0,0,5,0"/>
                        <Button Content="Сброс" Click="ResetCustomCommands_Click" Width="120" Height="35" Margin="0,0,5,0"/>
                        <Button Content="Сохранить и применить" Click="SaveCustomCommands_Click" Width="200" Height="35" FontWeight="Bold"/>
                    </WrapPanel>
                </Grid>
            </TabItem>
            
             <!-- Tab 3: Tools -->
            <TabItem Header="ИНСТРУМЕНТЫ">
                <StackPanel Margin="20,5,20,20">
                     <TextBlock Text="ДОСТУПНЫЕ УТИЛИТЫ" Foreground="#666666" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                     <Grid Margin="0,0,0,10">
                         <Grid.ColumnDefinitions>
                             <ColumnDefinition Width="Auto"/>
                             <ColumnDefinition Width="*"/>
                             <ColumnDefinition Width="Auto"/>
                         </Grid.ColumnDefinitions>
                         <StackPanel Grid.Column="0">
                             <Button Content="OrderCheck" Command="{Binding ToolsViewModel.OpenOrderCheckCommand}" HorizontalAlignment="Left" Width="250" Margin="0,0,0,5"/>
                             <Button Content="CLEAR.bat" Command="{Binding ToolsViewModel.OpenClearBatCommand}" HorizontalAlignment="Left" Width="250" Margin="0"/>
                         </StackPanel>
                         <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                             <Button Content="POS-ник M1" Command="{Binding ToolsViewModel.CopyPosM1LinkCommand}" Width="150" Margin="0,0,0,5"/>
                             <Button Content="POS-ник M" Command="{Binding ToolsViewModel.CopyPosLinkCommand}" Width="150" Margin="0"/>
                         </StackPanel>
                         <StackPanel Grid.Column="2">
                             <Button Content="FTP" Command="{Binding ToolsViewModel.OpenFtpCommand}" Width="250" Height="20" FontSize="14" Padding="0" ToolTip="Открыть \\files.resto.lan\" HorizontalAlignment="Right" Margin="0,0,0,5"/>
                             <Button Content="Открыть лог Helpera" Command="{Binding ToolsViewModel.OpenLogFolderCommand}" HorizontalAlignment="Right" Width="250" Height="20" FontSize="14" Padding="0" ToolTip="Открыть папку с логами" Margin="0"/>
                         </StackPanel>
                     </Grid>

                     <StackPanel Orientation="Horizontal" Margin="5,20,0,10">
                         <TextBlock Text="АВТО-ВХОД CRM (30 МИН)" Foreground="#666666" FontSize="12" FontWeight="Bold"/>
                         <TextBlock Text="Сначала тыкни Настройка!" Foreground="{DynamicResource BrushAccent}" FontSize="12" Margin="10,0,0,0" FontWeight="Bold"/>
                         <TextBlock x:Name="txtLastRun" Foreground="#888888" FontSize="12" Margin="10,0,0,0"/>
                     </StackPanel>
                     
                      <StackPanel Orientation="Horizontal" Margin="5,0,0,5">
                          <TextBlock Text="Логин:" VerticalAlignment="Center" Foreground="#666666" Margin="0,0,5,0" Width="45"/>
                          <TextBox x:Name="txtCrmLogin" Style="{StaticResource ThemedTextBox}" Width="150" Margin="0,0,15,0" ToolTip="Логин для CRM" 
                                   Text="{Binding CrmViewModel.CrmLogin, UpdateSourceTrigger=PropertyChanged}"/>
                          <TextBlock Text="Пароль:" VerticalAlignment="Center" Foreground="#666666" Margin="0,0,5,0"/>
                          <Grid Width="120">
                              <PasswordBox x:Name="txtCrmPassword" Style="{StaticResource ThemedPasswordBox}" ToolTip="Пароль для CRM" LostFocus="txtCrmPassword_LostFocus"/>
                              <TextBox x:Name="txtCrmPasswordVisible" Style="{StaticResource ThemedTextBox}" ToolTip="Пароль для CRM" Visibility="Collapsed" 
                                       Text="{Binding CrmViewModel.CrmPassword, UpdateSourceTrigger=PropertyChanged}"/>
                          </Grid>
                          <ToggleButton x:Name="btnShowPassword" Content="👁" Width="30" Margin="5,0,0,0" ToolTip="Показать/скрыть пароль" Click="btnShowPassword_Click"/>
                      </StackPanel>

                      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                          <ComboBox x:Name="cmbBrowsers" Style="{StaticResource ThemedComboBox}" Width="140" Margin="5,5,10,5" Height="35"
                                    DisplayMemberPath="Name" SelectedValuePath="Path"
                                    ItemsSource="{Binding CrmViewModel.Browsers}"
                                    SelectedItem="{Binding CrmViewModel.SelectedBrowser}"
                                    ToolTip="Выберите активный браузер"/>
                          <Button Content="↻" Command="{Binding CrmViewModel.RefreshBrowsersCommand}" Width="30" Height="35" Margin="0,5,10,5" Padding="0" ToolTip="Обновить список браузеров" FontSize="18"/>
                          <Button x:Name="btnCrmAutoLogin" Content="{Binding CrmViewModel.IsAutoLoginActive, Converter={StaticResource BoolToStringConverter}, ConverterParameter='ВЫКЛЮЧИТЬ|ВКЛЮЧИТЬ'}" 
                                  Command="{Binding CrmViewModel.ToggleAutoLoginCommand}" Width="100"/>
                          <Button Content="Настройка" Click="BtnCrmSettings_Click" Width="100"/>
                      </StackPanel>
                      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                          <Button Content="Проверить порт" Command="{Binding CrmViewModel.CheckPortCommand}" Width="100" Height="25" FontSize="11" Padding="0" ToolTip="Проверить доступность порта 9222"/>
                      </StackPanel>
                      <TextBlock x:Name="txtCrmStatus" Text="{Binding CrmViewModel.CrmStatus}" Foreground="#555" FontSize="12" Margin="5,-30,0,0" HorizontalAlignment="Left"/>

                      <TextBlock Text="ЛОГ ДЕЙСТВИЙ CRM:" Foreground="#666666" FontSize="12" Margin="5,0,0,5" FontWeight="Bold"/>
                      <TextBox x:Name="txtLog" Style="{StaticResource ThemedTextBox}" Height="120" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" FontFamily="Consolas" FontSize="11" 
                               Text="{Binding CrmViewModel.Log}" Foreground="{DynamicResource BrushLogForeground}" Padding="5"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- Counter (Moved below TabControl to be on top in Z-order) -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,20,0">
            <CheckBox x:Name="chkAltBlocker" 
                      Content="Блок Alt" 
                      IsChecked="{Binding IsAltBlockerEnabled}"
                      Command="{Binding ToggleAltBlockerCommand}"
                      VerticalAlignment="Center" 
                      Margin="0,0,15,0"
                      ToolTip="Блокирует открытие меню при нажатии Alt (для браузеров)"/>
            
            <Border Background="{DynamicResource BrushCounterBackground}" BorderBrush="{DynamicResource BrushCounterBorder}" BorderThickness="1" CornerRadius="4" Padding="15,5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Счетчик: " Foreground="{DynamicResource BrushCounterLabel}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="txtCommandCount" Text="{Binding CommandCount}" Foreground="{DynamicResource BrushCounterValue}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="{DynamicResource ColorAccent}" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <Button Content="↺" Command="{Binding ResetCommandCountCommand}" Margin="10,0,0,0" Padding="8,2" FontSize="14" VerticalAlignment="Center" ToolTip="Сбросить счетчик"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Quick Replies Input Mode Selector -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="80,0,0,5">
            <TextBlock Text="Ввод" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="11">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#888888"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=togglePasteMode, Path=IsChecked}" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource BrushAccent}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <CheckBox x:Name="togglePasteMode"
                          IsChecked="{Binding SettingsViewModel.UsePasteModeForQuickReplies}"
                          ToolTip="Режим ввода текста: Ввод (выкл) или Вставка (вкл)"
                          Style="{StaticResource Win11ToggleStyle}" />
            <TextBlock Text="Вставка" VerticalAlignment="Center" Margin="5,0,0,0" FontSize="11">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#888888"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=togglePasteMode, Path=IsChecked}" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource BrushAccent}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!-- Update Link (Bottom Left) -->
        <TextBlock x:Name="txtUpdateLink" Text="Обновить" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                   Margin="15,5,5,5" Foreground="#666666" FontSize="11" Cursor="Hand" MouseLeftButtonDown="TxtUpdateLink_Click">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource BrushAccent}"/>
                            <Setter Property="TextDecorations" Value="Underline"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Update Progress -->
        <ProgressBar x:Name="pbUpdate" Width="150" Height="4" VerticalAlignment="Bottom" HorizontalAlignment="Center"
                     Margin="0,0,0,10" Visibility="Collapsed"
                     Background="#25252A" BorderBrush="#3E3E42" Foreground="{DynamicResource BrushAccent}"/>

        <!-- Credits -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,5,5">
            <Button Content="Сбросить темы" Click="BtnResetThemes_Click"
                    Padding="5,0" Margin="0,0,10,0" VerticalAlignment="Center"
                    FontSize="11" BorderThickness="0" Background="Transparent" Foreground="#666666"
                    ToolTip="Сбросить цвета тем к значениям по умолчанию.&#x0a;Внимание: Пользовательские настройки цветов будут утеряны!"/>
            <Button x:Name="btnThemeSwitch" Content="☀" Click="BtnThemeSwitch_Click"
                    Width="20" Padding="0" Margin="0,0,10,0" VerticalAlignment="Center"
                    FontSize="12" BorderThickness="0" Background="Transparent" Foreground="#666666"
                    ToolTip="Сменить тему (Светлая/Темная)"/>
            <TextBlock Text="by Yustus  " VerticalAlignment="Center"
                       Foreground="#666666" FontSize="11" IsHitTestVisible="False"/>
        </StackPanel>
    </Grid>
</Window>